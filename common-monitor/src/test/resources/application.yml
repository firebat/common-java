spring:
  appliction:
    name: common-monitor-test

  profiles:
    active: test

  main:
    banner-mode: "off"

server:
  address: 127.0.0.1
  port: 8001

logging:
  level:
    org.springframework.test: WARN

common:
  monitor:
    url: '/monitor.html'
---
spring:
  profiles: test

