spring:
  appliction:
    name: common-monitor-test

  profiles:
    active: test

  main:
    banner-mode: "off"

logging:
  level:
    org.springframework.test: WARN

common:
  monitor:
    url: '/monitor.html'
---
spring:
  profiles: test

